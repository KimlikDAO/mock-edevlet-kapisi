{
  "private": true,
  "name": "oauth2test",
  "version": "1.0.0",
  "description": "OAuth2 test sunucusu",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "KimlikDAO <dao@kimlikdao.org>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
